export const metadata = {
    title: 'Sign Up | Store Findy',
    description: 'Create your free Store Findy account and build a store locator for your website in minutes — no credit card required.',
};

export default function SignUpLayout({ children }) {
    return children;
}
